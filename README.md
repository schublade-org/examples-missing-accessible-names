# Missing accessible names

Focuses on name computation: empty buttons, a CSS-only icon button, a whitespace link, and a search field with no label. The search story’s placeholder control is empty by default so `control-name` fires; type a placeholder to see it clear.

```bash
npx schublade serve --config ./schublade.toml
# or ./run.sh
```

http://127.0.0.1:47303
